[project]
name = "chat-app"
version = "0.1.0"
description = "Codebase concierge — a chat UI over a Claude Agent SDK agent"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.2.121",
    "fastapi>=0.115",
    "uvicorn>=0.32",
]

[tool.uv]
package = false
